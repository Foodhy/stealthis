---
// Resource detail pages are English-only to stay under Cloudflare Pages'
// 20,000-file deployment cap. Localized resource cards link to the canonical
// /r/<slug> page (see ResourceCard/ShowcaseCard/CollectionExplorer).
export function getStaticPaths() {
  return [];
}
---
