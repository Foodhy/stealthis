---
slug: otp-input
title: OTP / PIN Input
description: One-time password input with individual digit boxes, auto-advance on type, paste support, backspace to go back, and length variants (4 and 6 digits).
category: ui-components
type: component
tags: [otp, pin, code, input, verification, form]
tech: [css, vanilla-js]
difficulty: med
targets: [html]
license: MIT
createdAt: 2026-03-01
updatedAt: 2026-03-01
author:
  name: "Stealthis"
  src: "https://github.com/Foodhy/stealthis"
---

## OTP / PIN Input

Individual digit input boxes with auto-advance, backspace navigation, and paste support. Four variants cover the most common verification UX patterns.

### Variants

- **6-digit OTP** — default verification code input
- **4-digit PIN** — shorter PIN entry
- **6-digit error** — red-bordered invalid state with error message
- **6-digit success** — green-bordered confirmed state

### How it works

1. Each box is an `<input maxlength="1" inputmode="numeric">` in a flex row
2. On a valid digit keypress, the value is set and focus moves to the next box
3. On `Backspace`, if the current box is empty, focus moves to the previous box
4. On `paste`, the pasted string is split across boxes left-to-right; excess characters are ignored
5. When the last digit is filled, the group dispatches a `complete` event with the full code value

### States

- **Default** — neutral border, accent focus ring
- **Error** — red border on all boxes, shake animation, error label below
- **Success** — green border on all boxes, checkmark label below
