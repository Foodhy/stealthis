---
slug: chart-geo-dist
title: Geographic Distribution Chart
description: A geographic distribution chart with a country map and horizontal region bars. Features configurable regions, animated bars, color-coded map areas, and a responsive layout for dashboard analytics.
category: ui-components
type: component
targets: [html]
labRoute: /ui-components/chart-geo-dist
tags: [chart, data-visualization, svg, geo-chart, map, animated]
tech: [vanilla-js, svg]
difficulty: hard
author:
  name: "Stealthis"
  src: "https://github.com/Foodhy/stealthis"
createdAt: 2026-03-15
updatedAt: 2026-03-15
---

## Geographic Distribution Chart

A configurable geographic distribution chart combining a real SVG map with animated horizontal bar charts. Designed for dashboard analytics showing regional data breakdowns.

### Features

- **Real SVG map** — Uses a detailed Colombia departments map from [MapSVG](https://mapsvg.com/maps/colombia) (Creative Commons)
- Two-column layout: interactive SVG map on the left, horizontal bar chart on the right
- Departments are grouped into 10 macro-regions and colored by value
- Animated bar chart with staggered entrance animations
- Hover sync — hovering a bar highlights its departments on the map, and vice versa
- Tooltip shows department name and region on map hover
- Responsive layout — stacks vertically on narrow viewports
- Zero dependencies — vanilla JS

### Customization

Edit the `REGIONS` array in `script.js` to change region names, values, and which departments belong to each region. Each region has a `depts` array with department IDs matching the SVG (e.g. `CO-ANT` for Antioquia).

To use a **different country**, replace the SVG file and update the `REGIONS` array with the new department/region IDs from your SVG.

### Map Source

The Colombia SVG map is from [MapSVG — Colombia](https://mapsvg.com/maps/colombia), licensed under Creative Commons.

### Color Palette

The green gradient ranges from `#064e3b` (highest value) to `#86efac` (lowest value), automatically interpolated based on each region's relative value.
