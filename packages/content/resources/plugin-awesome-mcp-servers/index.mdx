---
slug: plugin-awesome-mcp-servers
title: "awesome-mcp-servers (wong2) — Curated List"
description: A community-curated GitHub list cataloging Model Context Protocol servers so developers can discover MCP integrations for Claude, Cursor and other AI tools.
category: plugins
collections: [ai, devtools]
type: component
tags: [mcp, directory, marketplace, claude-code, cursor, plugins, awesome-list]
tech: [markdown]
difficulty: easy
targets: [html]
license: MIT
author:
  name: "wong2"
  src: "https://github.com/wong2/awesome-mcp-servers"
createdAt: 2026-06-22
updatedAt: 2026-06-22
---

## What it is

`awesome-mcp-servers` by wong2 is a curated, community-maintained GitHub repository that
lists Model Context Protocol (MCP) servers. MCP is the open standard AI coding tools use to
connect to external data sources and tools, so this list works as a discovery directory for
finding ready-made servers to plug into your AI workflow. It is a reference list, not a
package registry — each entry links out to the server's own repo or docs.

A companion hosted index is published at [mcpservers.org](https://mcpservers.org/), which
presents the same curated entries in a browsable web UI.

## How to access / install

The list lives on GitHub. Browse it directly or clone it for offline reference:

```bash
# Open in the browser
open https://github.com/wong2/awesome-mcp-servers

# Or clone the list
git clone https://github.com/wong2/awesome-mcp-servers.git
```

Each listed server has its own installation steps. A typical MCP server is registered with a
client such as Claude Code via its CLI, for example:

```bash
claude mcp add <server-name> -- npx -y <server-package>
```

Refer to the individual server's README for the exact command and required configuration.

## Good for

- Discovering MCP servers for databases, APIs, browsers, file systems and more.
- Comparing official versus community implementations before wiring one into your editor.
- Onboarding to the MCP ecosystem across clients like Claude Code, Cursor and other
  MCP-compatible tools.
