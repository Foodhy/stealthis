---
slug: "lg-38-infinite-marquee-gsap-wrong-value-mcq-1b8b0883"
topic: "marquee"
title: "Spot the wrong CSS value in Infinite Marquee (GSAP)"
type: "identify"
difficulty: 1
resourceSlug: "lg-38-infinite-marquee-gsap"
estimatedSeconds: 20
quality: "published"
tags:
  - "generated"
  - "css"
  - "identify"
createdAt: "2026-05-22"
updatedAt: "2026-05-22"
code: |
  *,
  *::before,
  *::after {
    box-sizing: border-box-wrong;
    margin: 0;
    padding: 0;
  }
  body {
    background: #050910;
    color: #f2f6ff;
    font-family: system-ui, sans-serif;
    overflow: hidden;
  }
  
  .page {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 2.5rem;
  }
  
  /* ── Row container ── */
  .marquee-row {
    overflow: hidden;
    white-space: nowrap;
    width: 100%;
  }
  
  /* ── Track ── */
  .marquee-track {
    display: inline-flex;
    align-items: center;
    gap: 1.5rem;
    will-change: transform;
  }
  
  /* ── Words ── */
  .marquee-word {
    font-size: clamp(1.8rem, 5vw, 4rem);
    font-weight: 800;
    letter-spacing: -0.02em;
    color: rgba(255, 255, 255, 0.08);
  }
  
  .marquee-sep {
    font-size: 1.5rem;
    color: #1e293b;
  }
  
  /* ── Outline row ── */
  .marquee-row--outline .marquee-word {
    color: transparent;
    -webkit-text-stroke: 1px rgba(255, 255, 255, 0.12);
  }
  
  /* ── Fast row ── */
  .marquee-row--fast .marquee-word {
    font-size: clamp(1.2rem, 3vw, 2.5rem);
    color: rgba(255, 255, 255, 0.05);
  }
  
  .marquee-word--gradient {
    background: linear-gradient(90deg, #38bdf8, #818cf8, #f472b6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    opacity: 0.4;
  }
  
options:
  - "`box-sizing: border-box-wrong;` should be `box-sizing: border-box;`"
  - "The selector should be removed entirely"
  - "The declaration should use !important"
  - "Nothing is wrong"
answer: 0
explanation: "The mutated value `border-box-wrong` is incorrect for this snippet."
realWorldHook: "Spot the line that caused the regression. (marquee)"

---

Generated challenge.
